version: '3.8'
services:
  ai-mad:
    build: .
    container_name: ai-mad
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./data:/usr/src/app/data
    restart: unless-stopped
